// Create a new class called Vehicle
// It should include a property called wheels
// The constructor should set the number of wheels

export let vehicle;
// Create a new instance of a Vehicle assigned to vehicle
// Give the vehicle 4 wheels

// Create an extension class to Vehicle class called Car
// Car class should default the number of wheels in the constructor to 4 but still allow other numbers

// Create an extension class to Vehicle class called Bike
// Bike class should default the number of wheels to 2 but allow other numbers as well

// Create a new instance of car using Car class and a new instance of bike using Bike class
export let car;

export let bike;
