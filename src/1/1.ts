/**
 * Append code to the following statements in order to satisfy the tests in 1.test.ts
 *
 * For the moment, ignore the export keywords
 * it has been used to give the test file access to your answers
 */
export let myNumber;

export let myString;

export let myBoolean;

export let myArray;

export let myObject;

export let myFunction;
